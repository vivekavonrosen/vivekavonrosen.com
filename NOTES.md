# vivekavonrosen.com — Project Notes

Static HTML site in `site/`, deployed on Vercel, repo `github.com/vivekavonrosen/vivekavonrosen.com`.
Live at **www.vivekavonrosen.com** (apex 308→www, SSL good). Auto-deploys on push to `main`.
Deploy manually with: `npx vercel deploy --prod --yes` from project root.

## Key facts
- Brand: purple `#571F81` / gold `#DFB24A` / teal `#2C97AF` / cream `#FAF6EE`; Bebas Neue + Lato + Playfair.
- Pages: index, work-with-me, about, speaking, events, books, podcast, press, contact, blog. Shared styles in `site/css/styles.css`; entrance in `site/js/quantum.js`.
- **speaking.html is the exception** — fully self-contained (its own `<style>`), replicated from her vibecoded speaker page. Global style changes do NOT reach it.
- Photo library: `/Users/vivekavonrosen/Documents/Claude Cowork/IMages ` (note trailing space). Image-processing utility `remove_bg.py` (gitignored); PIL/numpy/scipy + standalone `ffmpeg` installed. No Homebrew on machine.
- Real links: Calendly discovery `calendly.com/vivekacoach/discovery-call`; Calendly speaking `calendly.com/vivekacoach/speaking-inquiry`; Skool `skool.com/womens-tech-collaborative-2106`; VIOS `visibilityos.tech`; YouTube `youtube.com/@Viveka_von_Rosen`; Substack `vivekavonrosen.substack.com`.

## Working
- Entrance: purple starfield + her real gold scrollwork backdrop (`scrollwork-bg.png`, mirrored top+bottom); tech words form above her name starting VIOS, cycle once (~21s), settle on the phoenix (sampled from `phoenix-mark.png`) and hold. Click = fast ~0.85s vortex → navigate ("I'm Ready for Traction"→work-with-me, "Show Me Around First"→home). Plays once/session, skippable, reduced-motion safe.
- Interior polish: portal dark sections (stars + scroll corners + ghost phoenix), card gold edges + hover, gold-pill CTAs, link underline sweeps, rung numbers 01/02/03, flourish dividers, branded photos on About + Work With Me.
- Articles page pulls live Substack posts via serverless `api/substack.js` (edge-cached 1hr, auto-updates, no redeploy).
- Speaking page: full replica with scrolling "As Seen In" marquee, 3 keynote cards, 4-image gallery (orange-jacket shot anchored top so head shows), downloadable one-sheet PDF at `site/files/`, site nav+footer added.
- Logo (phoenix bird) in every header + footer + favicon.
- **Analytics (2026-06-22):** Google Tag Manager `GTM-MBT56NQB` + GA4 `G-D9Y8X6ZYY5` installed directly in `<head>` of all 10 pages (gtag.js loaded directly so GA4 Realtime works immediately). NOTE: do NOT also add a GA4 tag inside the GTM container — would double-count. Confirmed live in GA4 Realtime.
- **Homepage hero (2026-06-22 redesign):** rebuilt to match the original beyondthedreamboard.com look — full-width purple-gradient banner (`.hero--purple`, scoped so about/speaking/work-with-me are untouched), photo on the LEFT anchored flush to the bottom edge, near-white radial glow behind her, white title-case "Turn Your Wisdom / Into Traction" on two lines, name/titles caption overlaying her lower body, two CTAs (white "Take the First Step" + outline "See the Ways to Work Together"). Photo is `viv-hero-cutout.png` — her purple-Chanel half-body shot from Drive, AI-cut-out (rembg `isnet-general-use` + white-fringe removal so the gaps between fingers are clean), ~160KB. Old `viv-hero.png` now unused.
- **Photo heroes (2026-06-25):** four interior pages now share a purple-washed photo-hero band — Work With Me (`.hero--photo`, facilitating shot), About (`.hero--about`, copy left + subject anchored right, trimmed from left), Events (`.hero--events`, centered), Blog (`.hero--blog`, centered, journalling shot). All use the same recipe: optimized to 1800px-wide JPG (~70–135KB) + a purple duotone wash baked in PIL (black `(38,14,64)` → white `(226,212,244)`, 15% original blended back) + a purple gradient overlay + gold corner scrollwork. Each page gets its own `.hero--<page>` background override in `styles.css`; markup is `class="hero center hero--photo hero--<page>"` (drop `center` and add `.hero-copy` for the About left-aligned variant). Source recipe lives in the session log. Old `viv-barb-bg.jpg` no longer referenced.
- **Visible Authority Intensive page + Stripe checkout (2026-06-25):** new page `site/visible-authority-intensive.html` (her real $2,000 / three-session offer, "pay first then book"). Reuses `.hero center hero--photo` (facilitating bg, no new image). Buttons go to a **live Stripe Payment Link** `https://buy.stripe.com/6oUaEW40J2PWcQz21Q8AE05`. Stripe objects in her account `acct_1RAyZvBSVzpeo3xT` (all LIVE mode): product `prod_UlsgP7WcOVekr4`, price `price_1TmKvUBSVzpeo3xThLQDeXNB` ($2,000), coupon `L7xbgt33` ($500 off, once), promo code `VIOSGRAD` → $1,500. Payment link has `allow_promotion_codes:true` + a hosted-confirmation message pointing to her Calendly `calendly.com/vivekacoach/private-with-viv` so buyers schedule after paying. Connected via the Stripe MCP (`plugin:small-business:stripe`, OAuth) — re-auth each session; the real write tools appear only after auth. Stripe edits made via `stripe_api_write` (operation IDs `PostProducts`/`PostPrices`/`PostCoupons`/`PostPromotionCodes` [needs `promotion.type:coupon` + `promotion.coupon`]/`PostPaymentLinks`). The Work With Me "Visible Authority Intensive" card now links here (was a dead-end to contact.html). Also fixed two Work With Me bugs same day: hero "See the Ways to Work Together" button pointed at non-existent `#pathway` → now `#wtic` (Rung One); "Join WTIC Pro" now links to the Skool premium-upgrade URL (`how-to-upgrade-to-premium?p=9677ddae`) instead of the general Skool page. `.center .hero-cta{ justify-content:center }` added so centered-section CTAs actually center. All verified live.
- **LinkedIn offer page (2026-06-25):** `site/linkedin.html`, added to the header nav between Events and Articles on every page (label "LinkedIn"). Consolidates the two BTDB LinkedIn pages into two offers: VIP Profile Optimizer $197 (self-paced → straight to her Lovable app `vvr-vip-profile.lovable.app`, which collects payment itself) and LinkedIn Legacy Launch $895 (90-min 1:1 → **Stripe checkout** `buy.stripe.com/cNi3cueFncqw8Aj9ui8AE06`, product `prod_UltC2psdGlmrXq`, price `price_1TmLQUBSVzpeo3xTQtkYQ8vB`; pay → hosted-confirmation hands them Calendly `linkedin-legacy`). Reuses `.hero center hero--photo` (facilitating bg).
- **WTIC page (2026-06-25):** `site/wtic.html` — "WTIC: Women's Technology Integration Collaborative". Hero reuses the events collaboration photo via `.hero--wtic` in styles.css. Linked from Work With Me Rung One ("See everything inside WTIC →") and the footer; not in the top nav. **v2 (same day):** rebuilt with her final voice-checked copy from a separate Claude-Chat build (`WTIC landing page/wtic-project`) and split the Pro pricing into Monthly $97 → Skool upgrade link, and **Annual $697/yr → her OWN Stripe checkout** (saves Skool's 10% fee). Free + hero + final CTAs still → Skool community `skool.com/womens-tech-collaborative-2106`.
  - **Stripe (LIVE) for the annual:** product `prod_UmBuUXPDQNQaSi`, recurring price `price_1TmdXkBSVzpeo3xT5JTuzXfT` ($697/yr, auto-renews), payment link `https://buy.stripe.com/7sY8wO68ReyEdUD21Q8AE07` (custom field `skoolemail` asks which email they'll use on Skool; confirmation = "watch your inbox").
  - **Webhook pipeline:** `api/stripe-webhook.js` (from the Chat build, ESM, fits the repo). On `checkout.session.completed` it emails her (via Resend) the buyer's name + Skool email + a button to the Skool members page so she can manually invite them (she's on Skool Hobby → invites are manual; Skool invite bypasses paywall → instant Pro). Ignores silent yearly renewals. Added root `package.json` (`type:module`, deps `stripe`+`resend`) so Vercel installs deps for the function; `substack.js` is already ESM so it still works.
  - **MANUAL STEPS SHE MUST DO (not done yet):** (1) upload wtic.html + api/stripe-webhook.js + package.json to GitHub; (2) Vercel → Settings → Env Vars: `STRIPE_SECRET_KEY` (sk_live), `RESEND_API_KEY`, `FROM_EMAIL` (on her verified Resend domain), `NOTIFY_EMAIL` (her inbox), and after step 3 `STRIPE_WEBHOOK_SECRET`; (3) after deploy, Stripe → Developers → Webhooks → add endpoint `https://www.vivekavonrosen.com/api/stripe-webhook`, event `checkout.session.completed`, copy signing secret into the env var, redeploy; (4) backstop: Stripe → Notifications → enable "Successful payments"; (5) test via Stripe Dashboard "Send test webhook" (checkout.session.completed) to confirm the Resend email arrives — avoids a real $697 charge. $97/month stays NATIVE in Skool — do not move it to Stripe. Annual is non-refundable (in the FAQ). I never handle her secret keys — she pastes them into Vercel's UI.
  - **WTIC is her current priority push (2026-06-25):** the ENTRANCE "I'm Ready for Traction" button now routes to `wtic.html` (was work-with-me.html) — changed via `data-dest` on `#enter-btn` in index.html. To avoid stranding people, wtic.html has an "Other Ways to Work With Me" ghost button at the bottom → work-with-me.html (plus the normal top nav on every page). Final copy was revised again (new headline "You know your business. Does your visibility match it? Technology can help.", new lead, reworked AI/integration paragraphs); Annual $697 is the gold/primary Pro button, Monthly secondary.
- **Featured-in marquee (2026-06-22):** replaced the white-on-white press-logo strip with a scrolling CSS marquee (`.featured-in` / `.marquee`), purple-on-white, gold ✦ separators, in its own tall centered band: Forbes · Inc. · TopRank Marketing · Small Business Trends · Money Magazine · Fast Company · Social Media Examiner · HubSpot · LinkedIn Learning. Pauses on hover, reduced-motion safe. The old dark "undervalued resource is the wisdom of experienced women" authority banner was removed. Old `logo-*.png` press images now unused.
- **Legal pages (2026-06-27):** `site/privacy.html` + `site/terms.html` built and linked from the footer on every page (the old `#` placeholders are gone). Podcast Spotify/Apple/YouTube links finalized (real Grey Matters pages + her YouTube channel/playlist). Plus a round of general polish.

## Still broken / open
- Old blog archive (63 beyondthedreamboard posts) not migrated — Articles page is Substack-only by design.
- Email: `viveka@vivekavonrosen.com` now RECEIVES via ImprovMX (free) → forwards to vivekavr@gmail.com. DNS at GoDaddy (MX mx1/mx2.improvmx.com 10/20 + SPF `v=spf1 include:spf.improvmx.com ~all`); ImprovMX domain verified green; alias is a catch-all/`viveka`. SENDING-as still needs paid SMTP (ImprovMX Premium $9/mo or Workspace) — replies currently go from gmail.com. Speaking page now DISPLAYS `viveka@vivekavonrosen.com` (swapped 2026-06-25); check other pages for any lingering beyondthedreamboard address. NOTE: GoDaddy is hard-blocked from browser tools — she must make DNS changes herself; I verify via `dig`.
- Grey-hair photo (`viv-grey.jpg`) is the only place it's used (Home "Who This Is For" portrait).
- Her image folder has unused landing-page mockups + lots of raw photo sources (the big PNGs/JPGs are working files; only the optimized `*-bg.jpg` versions are referenced by the site).
- Two unmerged Vercel PR branches sit on the remote — `vercel/install-vercel-web-analytics-*` and `vercel/install-vercel-speed-insights-*` (Vercel's UI offering its first-party analytics). NOT merged. Optional; we already have GA4 + GTM, so only add if she wants Vercel's built-in dashboards too. Decline/delete the branches if not.
- **Multi-channel editing causes drift (IMPORTANT for syncing):** the site gets edited from several places — this cloud session (`git`), local Claude/Cowork sessions on her Mac, GitHub web "Add files via upload", and GitHub Desktop. GitHub `main` is the single source of truth (= what Vercel deploys). Before editing here, ALWAYS `git fetch origin && git reset --hard origin/main` first, or you'll push a stale copy over newer work. When her GitHub Desktop shows pull/merge conflicts, the clean fix is to back up her local branch then reset it to `origin/main` (her computer is just a mirror; the live site is never at risk).

## Next step
Pick up wherever she directs. No blocking issues — site is fully live. Likely candidates: the rest of the BTDB gap-audit bucket 1 (long-form coaching page, VIOS page question), the WTC/lead-magnet pages, completing the Stripe webhook MANUAL STEPS for the WTIC annual checkout, or more design tweaks.

---

## Session Log

### 2026-06-14
Built the whole site this stretch. Highlights: deployed to www.vivekavonrosen.com and fixed the GoDaddy forwarding/DNS + SSL; redesigned the entrance several times landing on the starfield→phoenix version with her real gold scrollwork backdrop (she rejected a spinning-vortex redesign — reverted it); did interior visual passes 1–6 (portal sections, imagery, cards, buttons, rung numbers, dividers); wired Substack RSS into the Articles page via serverless function; doubled eyebrow/rung-label sizes; rebuilt the Speaking page twice — final version is a verbatim replica of her vibecoded speaker page (scrolling marquee, keynotes, one-sheet PDF, orange-jacket gallery image fixed to show her head). Repo restructured into `site/` and connected to Vercel for auto-deploy. Ended: site fully live, no blockers.

Later same day: set up email forwarding — added ImprovMX MX+SPF records at GoDaddy (she added them herself since GoDaddy is browser-blocked), verified ImprovMX green, confirmed receiving works to her Gmail. Removed the AI Base Camp tile from the Events page. Site displays beyondthedreamboard email still (swap pending). No blockers.

### 2026-06-22
Analytics + homepage hero work. (1) Installed Google Tag Manager (`GTM-MBT56NQB`) + GA4 (`G-D9Y8X6ZYY5`) snippets in every page's `<head>`; verified live in GA4 Realtime. (2) Redesigned the homepage hero to match the original beyondthedreamboard.com banner: full-width purple gradient, photo left anchored to the bottom, near-white glow, two-line title-case headline, name caption overlay, two CTAs — all scoped to `.hero--purple` so other pages are untouched. (3) Swapped in her new purple-Chanel half-body photo (pulled from Google Drive via shared link → `viv-hero-cutout.png`); came on a white background, so AI-cut it out with rembg (`isnet-general-use`) + white-fringe removal so the gaps between her fingers are clean. (4) Removed the dark "undervalued resource" authority banner and replaced the white-on-white press-logo strip with a scrolling purple-on-white "Featured in" marquee in a tall centered band. Worked directly on `main` per her request (she dislikes the feature-branch dance); each change previewed via headless-browser screenshots before shipping. All live, no blockers. NOTE: the two final commits show "Unverified" on GitHub — this environment's SSH commit-signing helper is non-functional (empty signing key); cosmetic only.

### 2026-06-25
Photo headers + two link fixes. (1) Podcast page Spotify/Apple/YouTube buttons swapped from the old beyondthedreamboard placeholders to the real Grey Matters pages; speaking page email display swapped to `viveka@vivekavonrosen.com`. (2) Built matching purple-washed photo heroes on four pages from photos she uploaded that day: Work With Me (replaced the viv-and-barb hero bg with the facilitating shot), About (rebuilt the two-column "Hi! I'm Viveka" hero into a photo hero — copy left over the open purple space, her figure anchored right, trimmed from the left), Events (new header, was text-only), Blog/Articles (new header, journalling shot). Recipe per image: PIL → resize to 1800px wide, duotone purple wash (black `(38,14,64)` → white `(226,212,244)`), blend 15% of the original back for warmth, save progressive JPG q82–84 (~70–135KB); each gets a `.hero--<page>` bg override in `styles.css`. Confirmed the empty Blog post grid in local preview is only because the static server can't run the `/api/substack` serverless function — live endpoint returns 200 with posts, so live is fine. Previewed every change in the headless browser before shipping. Delivered as a 10-file upload list for her to bring to GitHub (6 code: podcast/speaking/about/events/blog/styles.css; 4 images: the `*-bg.jpg` heroes). Raw source photos intentionally left out of the repo. NOTE: local `git push` still has no auth in this env — she publishes by uploading the changed files via the GitHub web UI, which triggers the Vercel auto-deploy.

### 2026-06-25 (later — BTDB gap audit + Visible Authority Intensive checkout)
Crawled beyondthedreamboard.com via its Squarespace sitemap (94 URLs, ~58 are the old blog archive that stays Substack-only by design). Used a subagent to read the ~28 substantive non-blog pages and produced a gap report vs vivekavonrosen.com. She chose three buckets to bring over: revenue/offer pages, the WTC community page, consolidated lead magnets (skipped retreats/webinar for now); plan written to `tasks/todo.md`. Started bucket 1 with the **Visible Authority Intensive** (her renamed Mini VIP offer): pulled the real copy + Calendly CTA from `/minivipsessions`, built the page, and — at her request — turned it into a **real Stripe checkout** (see the Working bullet for all the Stripe object IDs and the payment link). Page CTAs now go to Stripe, not Calendly; buyers get the Calendly link on the post-payment confirmation. Also fixed three Work With Me bugs she spotted: the Intensive card was a dead-end to contact.html (now → the new page), the hero "See the Ways to Work Together" button pointed at a non-existent `#pathway` anchor (now → `#wtic`), and "Join WTIC Pro" went to the general Skool page (now → the premium-upgrade URL). Shipped as a 3-file GitHub upload (visible-authority-intensive.html, work-with-me.html, styles.css); all verified live (200, Stripe button present, links resolve). Stripe is LIVE mode — real charges; do not run a test transaction unless she means it. Still open from the plan: the rest of bucket 1 (LinkedIn offer, long-form coaching), the WTC page, and lead magnets — and the question of whether other revenue pages should use the same Stripe-payment-link pattern, plus whether VIOS gets a page here or stays on `visibilityos.tech`.

### 2026-06-27 (analytics session + privacy/terms/podcast/polish wrap-up + sync rescue)
Two threads converged today. (A) Earlier in this cloud session I installed GTM + GA4 across all pages and did the homepage hero redesign / marquee work (logged under 06-22 above — the cloud container's clock reads a few days behind real time, so those commits are dated 06-22 but landed in this same stretch). (B) In separate local sessions she finished **privacy.html + terms.html** (now footer-linked site-wide, replacing the old `#` placeholders), finalized the **podcast** Spotify/Apple/YouTube links, and did general polish — all already live on GitHub when I synced. Spent the back half of the session **rescuing her GitHub Desktop sync**: her Mac's copy had drifted from GitHub (multi-channel editing), so a pull first hit an "uncommitted changes" wall, then a "4 conflicted files" merge. Walked her through Stash → Abort Merge → back up the local branch → `git reset --hard origin/main` so her computer cleanly mirrors the live site. Confirmed via the GitHub MCP + a live `curl` that GitHub `main` = the live site and contains everything (analytics, refined `hero--split` hero with the hand-drawn gold swash, marquee, the new linkedin/wtic/privacy/terms pages). Did NOT push my older cloud copy over the newer GitHub state — synced down to `origin/main` first, then only added this notes commit on top. Site fully live, no blockers.
